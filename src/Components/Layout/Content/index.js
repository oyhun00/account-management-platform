import React from 'react';
import { Layout, Tabs } from 'antd';

const ContentBox = () => {
  const { Content } = Layout;
  const { TabPane } = Tabs;

  return (
    <Content>
      123
    </Content>
  );
}

export default ContentBox;
